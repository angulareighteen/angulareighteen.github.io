kushal@fedora:~/src/angular/angulareighteen.github.io$ cd ~/src/angular/angulareighteen.github.io/; git rm karma.conf.js; git rm tslint.json; rm yarn.lock; time yarn install; yarn audit;
rm 'karma.conf.js'
rm 'tslint.json'
yarn install v1.22.22
info No lockfile found.
[1/4] Resolving packages...
warning @angular/animations@22.0.4: @angular/animations is deprecated. Use `animate.enter` and `animate.leave` instead. For more information see: https://v22.angular.dev/guide/animations.
[2/4] Fetching packages...
[3/4] Linking dependencies...
warning " > express-rate-limit@8.5.2" has unmet peer dependency "express@>= 4.11".
warning " > vitest@4.1.9" has unmet peer dependency "vite@^6.0.0 || ^7.0.0 || ^8.0.0".
[4/4] Building fresh packages...
success Saved lockfile.
Done in 17.30s.

real    0m17.446s
user    0m8.829s
sys	0m4.053s
yarn audit v1.22.22
┌───────────────┬──────────────────────────────────────────────────────────────┐
│ low           │ esbuild allows arbitrary file read when running the          │
│               │ development server on Windows                                │
├───────────────┼──────────────────────────────────────────────────────────────┤
│ Package       │ esbuild                                                      │
├───────────────┼──────────────────────────────────────────────────────────────┤
│ Patched in    │ >=0.28.1                                                     │
├───────────────┼──────────────────────────────────────────────────────────────┤
│ Dependency of │ @angular/build                                               │
├───────────────┼──────────────────────────────────────────────────────────────┤
│ Path          │ @angular/build > vite > esbuild                              │
├───────────────┼──────────────────────────────────────────────────────────────┤
│ More info     │ https://www.npmjs.com/advisories/1120680                     │
└───────────────┴──────────────────────────────────────────────────────────────┘
1 vulnerabilities found - Packages audited: 620
Severity: 1 Low
Done in 0.47s.
kushal@fedora:~/src/angular/angulareighteen.github.io$ cd ~/src/angular/angulareighteen.github.io/; cat update.sh; time bash update.sh;
#!/usr/bin/env bash
# =============================================================================
# update.sh — Toolchain update + rebuild + test + publish for this repo.
#
# Location: <repo root>/update.sh   (sits next to export.sh)
#
# Run with:   bash update.sh
#             bash /any/absolute/path/to/update.sh     # CWD does NOT matter
#
# What it does (modernised, while preserving every step of the original):
#   • Locates the repository from THIS script's own path, so the current
#     working directory is irrelevant (no hard-coded project directory).
#   • Bootstraps Node via fnm OR nvm, whichever is present (auto-detected;
#     force one with NODE_VM=fnm or NODE_VM=nvm in the environment).
#   • Operates on the CURRENT git branch — nothing is hard-coded to master.
#   • Writes its human-readable run log to docs/llm/update.md by default
#     (see UPDATE_LOG_REL below) instead of README.md.
#   • Logs unit-test output to locallog/fedoratest.md (as before).
#   • Appends network probes to ping.txt (as before).
#   • Upgrades Node to the latest LTS, installs the global @angular/cli + yarn,
#     records `ng version`, installs deps, runs `ng update`, refreshes the
#     browserslist + Baseline databases, builds for production, runs the unit
#     tests, bumps the patch version, runs export.sh to refresh
#     docs/llm/dump.txt, then rebases from and pushes to origin on the current
#     branch.
#
# ERROR-HANDLING NOTE:
#   This script intentionally does NOT use `set -e`. Like the original, it is a
#   best-effort pipeline: every checkpoint is committed and the run always
#   proceeds to the final push, even if an individual step (a flaky test, a
#   build warning treated as an error, an unreachable ping host, etc.) fails.
#   Fallible commands are individually guarded with `|| true`.
# =============================================================================

# ---------------------------------------------------------------------------
# Configuration — edit to taste
# ---------------------------------------------------------------------------
UPDATE_LOG_REL="docs/llm/update.md"      # main run log (replaces the old README.md target)
TEST_LOG_REL="locallog/fedoratest.md"    # unit-test output log
PING_LOG_REL="ping.txt"                  # network-probe log

NODE_MAX_OLD_SPACE_MB="8000"             # Node heap ceiling for the heavy Angular steps
PING_COUNT="20"                          # echo requests per probe
PING_TARGET="8.8.8.8"                    # probe target host

PROJECT_TITLE="angulareighteen"
SITE_URL="https://angulareighteen.github.io"
GH_SLUG="angulareighteen/angulareighteen.github.io"        # owner/repo used for the CI badge
WORKFLOW_FILE="firebase-hosting-merge.yml"

# Packages handed to `ng update`.
NG_UPDATE_PKGS=(@angular/core @angular/cli @angular/material)

# Node version manager selection: auto | fnm | nvm
NODE_VM="${NODE_VM:-auto}"

# ---------------------------------------------------------------------------
# 0. Resolve this script's own location, then the repo root (CWD-independent)
# ---------------------------------------------------------------------------
resolve_self() {
    local src="${BASH_SOURCE[0]}"
    if command -v readlink >/dev/null 2>&1 && readlink -f "$src" >/dev/null 2>&1; then
        readlink -f "$src"
        return
    fi
    # Portable fallback for systems without GNU `readlink -f`.
    local dir
    while [[ -h "$src" ]]; do
        dir="$(cd -P "$(dirname "$src")" >/dev/null 2>&1 && pwd)"
        src="$(readlink "$src")"
        [[ "$src" != /* ]] && src="$dir/$src"
    done
    printf '%s\n' "$(cd -P "$(dirname "$src")" >/dev/null 2>&1 && pwd)/$(basename "$src")"
}

SCRIPT_PATH="$(resolve_self)"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"
EXPORT_SH="${SCRIPT_DIR}/export.sh"

if ! REPO_ROOT="$(git -C "$SCRIPT_DIR" rev-parse --show-toplevel 2>/dev/null)"; then
    echo "ERROR: '$SCRIPT_DIR' is not inside a git repository; aborting." >&2
    exit 1
fi
cd "$REPO_ROOT" || { echo "ERROR: cannot cd into '$REPO_ROOT'." >&2; exit 1; }

CURRENT_BRANCH="$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo HEAD)"

# Absolute paths derived from the configurable relative paths above.
LOG="${REPO_ROOT}/${UPDATE_LOG_REL}"
TEST_LOG="${REPO_ROOT}/${TEST_LOG_REL}"
PING_LOG="${REPO_ROOT}/${PING_LOG_REL}"
mkdir -p "$(dirname "$LOG")" "$(dirname "$TEST_LOG")" "$(dirname "$PING_LOG")"

# Applied to every Node-heavy command below (exported once).
export NODE_OPTIONS="--max-old-space-size=${NODE_MAX_OLD_SPACE_MB}"

# ---------------------------------------------------------------------------
# Helpers
# ---------------------------------------------------------------------------
note() { printf '\n==> %s\n' "$*"; }                  # progress line to the terminal

gc() {                                                 # git checkpoint; never aborts the run
    git add -A
    git commit -m "$1" || true
}

md()         { printf '%s\n'   "$*"          >> "$LOG"; }   # append a line to the run log
md_code()    { printf '```%s\n' "${1:-}"     >> "$LOG"; }   # open a fenced block (``` or ```bash)
md_endcode() { printf '```\n'                >> "$LOG"; }   # close a fenced block

ping_first() { ping -A -D -c "$PING_COUNT" -v "$PING_TARGET" >  "$PING_LOG" 2>&1 || true; }
ping_probe() { ping -A -D -c "$PING_COUNT" -v "$PING_TARGET" >> "$PING_LOG" 2>&1 || true; }
ping_final() { ping    -D -c "$PING_COUNT" -v "$PING_TARGET" >> "$PING_LOG" 2>&1 || true; }

# --- Node version manager bootstrap (fnm OR nvm) ---------------------------
NODE_VM_ACTIVE="none"

_try_fnm() {
    command -v fnm >/dev/null 2>&1 || return 1
    local out
    # An explicit shell is faster and avoids fnm's process-tree detection.
    out="$(fnm env --shell bash 2>/dev/null)" || return 1
    eval "$out"
    NODE_VM_ACTIVE="fnm"
}

_try_nvm() {
    local nvm_sh="${NVM_DIR:-$HOME/.nvm}/nvm.sh"
    [[ -s "$nvm_sh" ]] || return 1
    # shellcheck disable=SC1090
    \. "$nvm_sh" || return 1
    command -v nvm >/dev/null 2>&1 || return 1
    NODE_VM_ACTIVE="nvm"
}

bootstrap_node_vm() {
    case "$NODE_VM" in
        fnm) _try_fnm || echo "WARN: NODE_VM=fnm requested but fnm is unavailable." >&2 ;;
        nvm) _try_nvm || echo "WARN: NODE_VM=nvm requested but nvm is unavailable." >&2 ;;
        auto|*)
            _try_fnm || _try_nvm \
                || echo "WARN: neither fnm nor nvm found; Node-dependent steps may fail." >&2 ;;
    esac
    note "Node version manager: ${NODE_VM_ACTIVE}"
}

install_use_lts() {
    case "$NODE_VM_ACTIVE" in
        fnm)
            time fnm install --lts             || true
            time fnm use lts-latest            || true
            fnm default lts-latest 2>/dev/null || true   # default for future shells
            ;;
        nvm)
            time nvm install --lts             || true
            time nvm use --lts                 || true
            ;;
        *)
            echo "WARN: no Node version manager active; skipping LTS install/use." >&2
            ;;
    esac
}

run_export() {
    if [[ -f "$EXPORT_SH" ]]; then
        note "running export.sh (refresh ${UPDATE_LOG_REL%/*}/dump.txt)"
        # export.sh tees its full dump to stdout; keep the console quiet and
        # rely on the file it writes to docs/llm/dump.txt.
        bash "$EXPORT_SH" >/dev/null 2>&1 || echo "WARN: export.sh exited non-zero." >&2
    else
        echo "WARN: export.sh not found at '$EXPORT_SH'; skipping context dump." >&2
    fi
}

# ===========================================================================
# 1. Boot: version manager + first probe + fresh run-log header
# ===========================================================================
bootstrap_node_vm

ping_first
date > "$LOG"
gc "add timestamp"

md ''
md "# ${PROJECT_TITLE}"
md ''
md ''
md "This project is live at [${SITE_URL}](${SITE_URL} \"colorado!\") thanks to Firebase."
md ''
md 'CI Status: '
md ''
md "[![Deploy to Firebase Hosting on merge](https://github.com/${GH_SLUG}/actions/workflows/${WORKFLOW_FILE}/badge.svg)](https://github.com/${GH_SLUG}/actions/workflows/${WORKFLOW_FILE})"
md ''

# ===========================================================================
# 2. System status
# ===========================================================================
md_code bash
ping_probe
gc "begin add system status"
md "System Memory"
if command -v free >/dev/null 2>&1; then
    free -h >> "$LOG" 2>&1 || true
else
    md '(free unavailable)'
fi
md "System Storage"
du -sh . >> "$LOG" 2>&1 || true
gc "add system status"
md_endcode
gc "end add system status"

# ===========================================================================
# 3. Update Node (LTS) + global tooling + install deps
# ===========================================================================
md_code bash
ping_probe
gc "begin update node"
install_use_lts
time npm install --global @angular/cli yarn || true
time yarn run ng version >> "$LOG" 2>&1 || true
time yarn >> "$LOG" 2>&1 || true
md_endcode
ping_probe
gc "end update node"

# ===========================================================================
# 4. Update Angular (output goes to the terminal, as in the original)
# ===========================================================================
gc "begin prepare to update angular"
time yarn run ng update "${NG_UPDATE_PKGS[@]}" || true
gc "end prepare to update angular"

# ===========================================================================
# 5. Refresh the browser-targeting databases
#    - caniuse-lite / browserslist   (update-browserslist-db)
#    - baseline-browser-mapping       (consulted by @angular/build; without a
#      refresh the build prints "the data in this module is over two months old")
#    `update-browserslist-db` only touches caniuse-lite, so the Baseline data is
#    refreshed separately. `yarn add -D` records it in package.json + yarn.lock;
#    npm is a fallback if yarn refuses for any reason.
# ===========================================================================
md_code bash
time npx update-browserslist-db@latest >> "$LOG" 2>&1 || true
time yarn add --dev baseline-browser-mapping@latest >> "$LOG" 2>&1 \
    || time npm install --save-dev baseline-browser-mapping@latest >> "$LOG" 2>&1 \
    || true
md_endcode

# ===========================================================================
# 6. Production build
# ===========================================================================
md_code bash
ping_probe
gc "begin prepare to build angular"
time yarn run ng build --configuration production >> "$LOG" 2>&1 || true
md_endcode
gc "end prepare to build angular"

# ===========================================================================
# 7. Unit tests (logged to the test log).
#    Angular 20+ runs unit tests with Vitest (builder: @angular/build:unit-test),
#    not Karma. `--watch=false` guarantees a single, non-interactive run in a
#    headless/CI context so the script never blocks in watch mode.
# ===========================================================================
printf '```bash\n' > "$TEST_LOG"
ping_probe
gc "begin prepare to unit test angular"
time yarn run ng test --watch=false >> "$TEST_LOG" || true
printf '```\n' >> "$TEST_LOG"
ping_probe
gc "end prepare to unit test angular"

# ===========================================================================
# 8. Version bump, context dump, and publish on the CURRENT branch
# ===========================================================================
date >> "$LOG"
ping_final
# NOTE: in Yarn Classic, `yarn version --patch` wants a clean tree and makes
# its own commit + tag. Command kept identical to the original.
time yarn version --patch >> "$LOG" 2>&1 || true
gc "add timestamp"

run_export
gc "add repo context dump"

if [[ "$CURRENT_BRANCH" == "HEAD" ]]; then
    echo "WARN: detached HEAD — skipping pull/push." >&2
else
    note "syncing with origin/${CURRENT_BRANCH}"
    git pull --rebase origin "$CURRENT_BRANCH" --strategy-option=ours || true
    gc "merge from remote"
    git push origin "$CURRENT_BRANCH" || true
fi

note "done."

==> Node version manager: fnm
[master 97ccdac] add timestamp
 2 files changed, 25 insertions(+), 319 deletions(-)
[master 8efa054] begin add system status
 2 files changed, 38 insertions(+)
[master 2a8011c] add system status
 1 file changed, 6 insertions(+)
[master bdf1b9a] end add system status
 1 file changed, 1 insertion(+)
[master 0b3663d] begin update node
 2 files changed, 28 insertions(+)
Installing Node v24.18.0 (x64)
warning: Version already installed at "/home/kushal/.local/share/fnm/node-versions/v24.18.0"

real	0m0.154s
user	0m0.005s
sys	0m0.009s
Using Node for alias lts-latest

real	0m0.002s
user	0m0.000s
sys	0m0.002s

changed 278 packages in 6s

69 packages are looking for funding
  run `npm fund` for details
npm warn allow-scripts 1 package has install scripts not yet covered by allowScripts:
npm warn allow-scripts   yarn@1.22.22 (preinstall: :; (node ./preinstall.js > /dev/null 2>&1 || true))
npm warn allow-scripts
npm warn allow-scripts Run `npm approve-scripts --allow-scripts-pending` to review, or `npm approve-scripts <pkg>` to allow.

real	0m5.589s
user	0m3.300s
sys	0m1.768s

real	0m0.452s
user	0m0.356s
sys	0m0.132s

real	0m0.212s
user	0m0.231s
sys	0m0.053s
[master 7268f55] end update node
 2 files changed, 70 insertions(+)
On branch master
Your branch is ahead of 'origin/master' by 8 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
yarn run v1.22.22
$ ng update @angular/core @angular/cli @angular/material
Using package manager: yarn
Collecting installed dependencies...
Found 555 dependencies.
Fetching dependency metadata from registry...
Package '@angular/core' is already up to date.
Package '@angular/cli' is already up to date.
Package '@angular/material' is already up to date.
Done in 3.34s.

real	0m3.446s
user	0m1.730s
sys	0m0.570s
On branch master
Your branch is ahead of 'origin/master' by 8 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean

real	0m1.020s
user	0m0.764s
sys	0m0.191s

real	0m1.120s
user	0m1.259s
sys	0m0.539s
[master fb4907c] begin prepare to build angular
 4 files changed, 53 insertions(+), 1 deletion(-)

real	0m3.692s
user	0m6.661s
sys	0m0.926s
warning: in the working copy of 'public/3rdpartylicenses.txt', CRLF will be replaced by LF the next time Git touches it
[master 251f3b8] end prepare to build angular
 14 files changed, 53 insertions(+), 1953 deletions(-)
 delete mode 100644 public/browser/chunk-BtyP_Wke.js
 delete mode 100644 public/browser/chunk-Cc02ckWq.js
 delete mode 100644 public/browser/chunk-CfqO4fnY.js
 delete mode 100644 public/browser/chunk-CguUs1Qa.js
 delete mode 100644 public/browser/chunk-Cigzcxaj.js
 delete mode 100644 public/browser/chunk-DDUXkZPy.js
 delete mode 100644 public/browser/chunk-DjbP6_O7.js
 delete mode 100644 public/browser/chunk-DjveFeZd.js
 delete mode 100644 public/browser/chunk-b_0CUpWM.js
 delete mode 100644 public/browser/chunk-kCSSTtWh.js
 delete mode 100644 public/browser/main-YS4WI2CN.js
warning: in the working copy of 'public/3rdpartylicenses.txt', CRLF will be replaced by LF the next time Git touches it
[master ca471a1] begin prepare to unit test angular
 2 files changed, 27 insertions(+), 33 deletions(-)

real	0m3.989s
user	0m21.095s
sys	0m4.151s
[master d4f27b5] end prepare to unit test angular
 2 files changed, 60 insertions(+)

real	0m0.155s
user	0m0.094s
sys	0m0.057s
[master 164feb7] add timestamp
 2 files changed, 32 insertions(+)

==> running export.sh (refresh docs/llm/dump.txt)
warning: in the working copy of 'docs/llm/dump.txt', CRLF will be replaced by LF the next time Git touches it
[master 7ec4450] add repo context dump
 1 file changed, 519 insertions(+), 2868 deletions(-)

==> syncing with origin/master
From github.com:angulareighteen/angulareighteen.github.io
 * branch            master     -> FETCH_HEAD
Current branch master is up to date.
warning: in the working copy of 'docs/llm/dump.txt', CRLF will be replaced by LF the next time Git touches it
On branch master
Your branch is ahead of 'origin/master' by 15 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
Enumerating objects: 115, done.
Counting objects: 100% (115/115), done.
Delta compression using up to 16 threads
Compressing objects: 100% (78/78), done.
Writing objects: 100% (92/92), 68.85 KiB | 5.74 MiB/s, done.
Total 92 (delta 57), reused 0 (delta 0), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (57/57), completed with 17 local objects.
remote: 
remote: GitHub found 48 vulnerabilities on angulareighteen/angulareighteen.github.io's default branch (15 high, 29 moderate, 4 low). To find out more, visit:
remote:      https://github.com/angulareighteen/angulareighteen.github.io/security/dependabot
remote: 
To github.com:angulareighteen/angulareighteen.github.io.git
   09322d9..7ec4450  master -> master

==> done.

real	0m50.359s
user	0m38.226s
sys	0m12.098s
kushal@fedora:~/src/angular/angulareighteen.github.io$ cd ~/src/angular/angulareighteen.github.io/; cat update.sh; time bash update.sh;
#!/usr/bin/env bash
# =============================================================================
# update.sh — Toolchain update + rebuild + test + publish for this repo.
#
# Location: <repo root>/update.sh   (sits next to export.sh)
#
# Run with:   bash update.sh
#             bash /any/absolute/path/to/update.sh     # CWD does NOT matter
#
# What it does (modernised, while preserving every step of the original):
#   • Locates the repository from THIS script's own path, so the current
#     working directory is irrelevant (no hard-coded project directory).
#   • Bootstraps Node via fnm OR nvm, whichever is present (auto-detected;
#     force one with NODE_VM=fnm or NODE_VM=nvm in the environment).
#   • Operates on the CURRENT git branch — nothing is hard-coded to master.
#   • Writes its human-readable run log to docs/llm/update.md by default
#     (see UPDATE_LOG_REL below) instead of README.md.
#   • Logs unit-test output to locallog/fedoratest.md (as before).
#   • Appends network probes to ping.txt (as before).
#   • Upgrades Node to the latest LTS, installs the global @angular/cli + yarn,
#     records `ng version`, installs deps, runs `ng update`, refreshes the
#     browserslist + Baseline databases, builds for production, runs the unit
#     tests, bumps the patch version, runs export.sh to refresh
#     docs/llm/dump.txt, then rebases from and pushes to origin on the current
#     branch.
#
# ERROR-HANDLING NOTE:
#   This script intentionally does NOT use `set -e`. Like the original, it is a
#   best-effort pipeline: every checkpoint is committed and the run always
#   proceeds to the final push, even if an individual step (a flaky test, a
#   build warning treated as an error, an unreachable ping host, etc.) fails.
#   Fallible commands are individually guarded with `|| true`.
# =============================================================================

# ---------------------------------------------------------------------------
# Configuration — edit to taste
# ---------------------------------------------------------------------------
UPDATE_LOG_REL="docs/llm/update.md"      # main run log (replaces the old README.md target)
TEST_LOG_REL="locallog/fedoratest.md"    # unit-test output log
PING_LOG_REL="ping.txt"                  # network-probe log

NODE_MAX_OLD_SPACE_MB="8000"             # Node heap ceiling for the heavy Angular steps
PING_COUNT="20"                          # echo requests per probe
PING_TARGET="8.8.8.8"                    # probe target host

PROJECT_TITLE="angulareighteen"
SITE_URL="https://angulareighteen.github.io"
GH_SLUG="angulareighteen/angulareighteen.github.io"        # owner/repo used for the CI badge
WORKFLOW_FILE="firebase-hosting-merge.yml"

# Packages handed to `ng update`.
NG_UPDATE_PKGS=(@angular/core @angular/cli @angular/material)

# Node version manager selection: auto | fnm | nvm
NODE_VM="${NODE_VM:-auto}"

# ---------------------------------------------------------------------------
# 0. Resolve this script's own location, then the repo root (CWD-independent)
# ---------------------------------------------------------------------------
resolve_self() {
    local src="${BASH_SOURCE[0]}"
    if command -v readlink >/dev/null 2>&1 && readlink -f "$src" >/dev/null 2>&1; then
        readlink -f "$src"
        return
    fi
    # Portable fallback for systems without GNU `readlink -f`.
    local dir
    while [[ -h "$src" ]]; do
        dir="$(cd -P "$(dirname "$src")" >/dev/null 2>&1 && pwd)"
        src="$(readlink "$src")"
        [[ "$src" != /* ]] && src="$dir/$src"
    done
    printf '%s\n' "$(cd -P "$(dirname "$src")" >/dev/null 2>&1 && pwd)/$(basename "$src")"
}

SCRIPT_PATH="$(resolve_self)"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"
EXPORT_SH="${SCRIPT_DIR}/export.sh"

if ! REPO_ROOT="$(git -C "$SCRIPT_DIR" rev-parse --show-toplevel 2>/dev/null)"; then
    echo "ERROR: '$SCRIPT_DIR' is not inside a git repository; aborting." >&2
    exit 1
fi
cd "$REPO_ROOT" || { echo "ERROR: cannot cd into '$REPO_ROOT'." >&2; exit 1; }

CURRENT_BRANCH="$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo HEAD)"

# Absolute paths derived from the configurable relative paths above.
LOG="${REPO_ROOT}/${UPDATE_LOG_REL}"
TEST_LOG="${REPO_ROOT}/${TEST_LOG_REL}"
PING_LOG="${REPO_ROOT}/${PING_LOG_REL}"
mkdir -p "$(dirname "$LOG")" "$(dirname "$TEST_LOG")" "$(dirname "$PING_LOG")"

# Applied to every Node-heavy command below (exported once).
export NODE_OPTIONS="--max-old-space-size=${NODE_MAX_OLD_SPACE_MB}"

# ---------------------------------------------------------------------------
# Helpers
# ---------------------------------------------------------------------------
note() { printf '\n==> %s\n' "$*"; }                  # progress line to the terminal

gc() {                                                 # git checkpoint; never aborts the run
    git add -A
    git commit -m "$1" || true
}

md()         { printf '%s\n'   "$*"          >> "$LOG"; }   # append a line to the run log
md_code()    { printf '```%s\n' "${1:-}"     >> "$LOG"; }   # open a fenced block (``` or ```bash)
md_endcode() { printf '```\n'                >> "$LOG"; }   # close a fenced block

ping_first() { ping -A -D -c "$PING_COUNT" -v "$PING_TARGET" >  "$PING_LOG" 2>&1 || true; }
ping_probe() { ping -A -D -c "$PING_COUNT" -v "$PING_TARGET" >> "$PING_LOG" 2>&1 || true; }
ping_final() { ping    -D -c "$PING_COUNT" -v "$PING_TARGET" >> "$PING_LOG" 2>&1 || true; }

# --- Node version manager bootstrap (fnm OR nvm) ---------------------------
NODE_VM_ACTIVE="none"

_try_fnm() {
    command -v fnm >/dev/null 2>&1 || return 1
    local out
    # An explicit shell is faster and avoids fnm's process-tree detection.
    out="$(fnm env --shell bash 2>/dev/null)" || return 1
    eval "$out"
    NODE_VM_ACTIVE="fnm"
}

_try_nvm() {
    local nvm_sh="${NVM_DIR:-$HOME/.nvm}/nvm.sh"
    [[ -s "$nvm_sh" ]] || return 1
    # shellcheck disable=SC1090
    \. "$nvm_sh" || return 1
    command -v nvm >/dev/null 2>&1 || return 1
    NODE_VM_ACTIVE="nvm"
}

bootstrap_node_vm() {
    case "$NODE_VM" in
        fnm) _try_fnm || echo "WARN: NODE_VM=fnm requested but fnm is unavailable." >&2 ;;
        nvm) _try_nvm || echo "WARN: NODE_VM=nvm requested but nvm is unavailable." >&2 ;;
        auto|*)
            _try_fnm || _try_nvm \
                || echo "WARN: neither fnm nor nvm found; Node-dependent steps may fail." >&2 ;;
    esac
    note "Node version manager: ${NODE_VM_ACTIVE}"
}

install_use_lts() {
    case "$NODE_VM_ACTIVE" in
        fnm)
            time fnm install --lts             || true
            time fnm use lts-latest            || true
            fnm default lts-latest 2>/dev/null || true   # default for future shells
            ;;
        nvm)
            time nvm install --lts             || true
            time nvm use --lts                 || true
            ;;
        *)
            echo "WARN: no Node version manager active; skipping LTS install/use." >&2
            ;;
    esac
}

run_export() {
    if [[ -f "$EXPORT_SH" ]]; then
        note "running export.sh (refresh ${UPDATE_LOG_REL%/*}/dump.txt)"
        # export.sh tees its full dump to stdout; keep the console quiet and
        # rely on the file it writes to docs/llm/dump.txt.
        bash "$EXPORT_SH" >/dev/null 2>&1 || echo "WARN: export.sh exited non-zero." >&2
    else
        echo "WARN: export.sh not found at '$EXPORT_SH'; skipping context dump." >&2
    fi
}

# ===========================================================================
# 1. Boot: version manager + first probe + fresh run-log header
# ===========================================================================
bootstrap_node_vm

ping_first
date > "$LOG"
gc "add timestamp"

md ''
md "# ${PROJECT_TITLE}"
md ''
md ''
md "This project is live at [${SITE_URL}](${SITE_URL} \"colorado!\") thanks to Firebase."
md ''
md 'CI Status: '
md ''
md "[![Deploy to Firebase Hosting on merge](https://github.com/${GH_SLUG}/actions/workflows/${WORKFLOW_FILE}/badge.svg)](https://github.com/${GH_SLUG}/actions/workflows/${WORKFLOW_FILE})"
md ''

# ===========================================================================
# 2. System status
# ===========================================================================
md_code bash
ping_probe
gc "begin add system status"
md "System Memory"
if command -v free >/dev/null 2>&1; then
    free -h >> "$LOG" 2>&1 || true
else
    md '(free unavailable)'
fi
md "System Storage"
du -sh . >> "$LOG" 2>&1 || true
gc "add system status"
md_endcode
gc "end add system status"

# ===========================================================================
# 3. Update Node (LTS) + global tooling + install deps
# ===========================================================================
md_code bash
ping_probe
gc "begin update node"
install_use_lts
time npm install --global @angular/cli yarn || true
time yarn run ng version >> "$LOG" 2>&1 || true
time yarn >> "$LOG" 2>&1 || true
md_endcode
ping_probe
gc "end update node"

# ===========================================================================
# 4. Update Angular (output goes to the terminal, as in the original)
# ===========================================================================
gc "begin prepare to update angular"
time yarn run ng update "${NG_UPDATE_PKGS[@]}" || true
gc "end prepare to update angular"

# ===========================================================================
# 5. Refresh the browser-targeting databases
#    - caniuse-lite / browserslist   (update-browserslist-db)
#    - baseline-browser-mapping       (consulted by @angular/build; without a
#      refresh the build prints "the data in this module is over two months old")
#    `update-browserslist-db` only touches caniuse-lite, so the Baseline data is
#    refreshed separately. `yarn add -D` records it in package.json + yarn.lock;
#    npm is a fallback if yarn refuses for any reason.
# ===========================================================================
md_code bash
time npx update-browserslist-db@latest >> "$LOG" 2>&1 || true
time yarn add --dev baseline-browser-mapping@latest >> "$LOG" 2>&1 \
    || time npm install --save-dev baseline-browser-mapping@latest >> "$LOG" 2>&1 \
    || true
md_endcode

# ===========================================================================
# 6. Production build
# ===========================================================================
md_code bash
ping_probe
gc "begin prepare to build angular"
time yarn run ng build --configuration production >> "$LOG" 2>&1 || true
md_endcode
gc "end prepare to build angular"

# ===========================================================================
# 7. Unit tests (logged to the test log).
#    Angular 20+ runs unit tests with Vitest (builder: @angular/build:unit-test),
#    not Karma. `--watch=false` guarantees a single, non-interactive run in a
#    headless/CI context so the script never blocks in watch mode.
# ===========================================================================
printf '```bash\n' > "$TEST_LOG"
ping_probe
gc "begin prepare to unit test angular"
time yarn run ng test --watch=false >> "$TEST_LOG" || true
printf '```\n' >> "$TEST_LOG"
ping_probe
gc "end prepare to unit test angular"

# ===========================================================================
# 8. Version bump, context dump, and publish on the CURRENT branch
# ===========================================================================
date >> "$LOG"
ping_final
# NOTE: in Yarn Classic, `yarn version --patch` wants a clean tree and makes
# its own commit + tag. Command kept identical to the original.
time yarn version --patch >> "$LOG" 2>&1 || true
gc "add timestamp"

run_export
gc "add repo context dump"

if [[ "$CURRENT_BRANCH" == "HEAD" ]]; then
    echo "WARN: detached HEAD — skipping pull/push." >&2
else
    note "syncing with origin/${CURRENT_BRANCH}"
    git pull --rebase origin "$CURRENT_BRANCH" --strategy-option=ours || true
    gc "merge from remote"
    git push origin "$CURRENT_BRANCH" || true
fi

note "done."

==> Node version manager: fnm
warning: in the working copy of 'docs/llm/dump.txt', CRLF will be replaced by LF the next time Git touches it
[master 9c44a8e] add timestamp
 2 files changed, 25 insertions(+), 333 deletions(-)
[master ec6c4e1] begin add system status
 2 files changed, 38 insertions(+)
[master 07b0911] add system status
 1 file changed, 6 insertions(+)
[master d228e7d] end add system status
 1 file changed, 1 insertion(+)
[master aea1d78] begin update node
 2 files changed, 28 insertions(+)
Installing Node v24.18.0 (x64)
warning: Version already installed at "/home/kushal/.local/share/fnm/node-versions/v24.18.0"

real	0m0.210s
user	0m0.006s
sys	0m0.009s
Using Node for alias lts-latest

real	0m0.002s
user	0m0.000s
sys	0m0.002s

changed 278 packages in 5s

69 packages are looking for funding
  run `npm fund` for details
npm warn allow-scripts 1 package has install scripts not yet covered by allowScripts:
npm warn allow-scripts   yarn@1.22.22 (preinstall: :; (node ./preinstall.js > /dev/null 2>&1 || true))
npm warn allow-scripts
npm warn allow-scripts Run `npm approve-scripts --allow-scripts-pending` to review, or `npm approve-scripts <pkg>` to allow.

real	0m5.358s
user	0m3.679s
sys	0m1.927s

real	0m0.383s
user	0m0.322s
sys	0m0.127s

real	0m0.218s
user	0m0.239s
sys	0m0.055s
[master e6815b7] end update node
 2 files changed, 70 insertions(+)
On branch master
Your branch is ahead of 'origin/master' by 6 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
yarn run v1.22.22
$ ng update @angular/core @angular/cli @angular/material
Using package manager: yarn
Collecting installed dependencies...
Found 555 dependencies.
Fetching dependency metadata from registry...
Package '@angular/core' is already up to date.
Package '@angular/cli' is already up to date.
Package '@angular/material' is already up to date.
Done in 3.27s.

real	0m3.372s
user	0m1.765s
sys	0m0.597s
On branch master
Your branch is ahead of 'origin/master' by 6 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean

real	0m1.021s
user	0m0.813s
sys	0m0.187s

real	0m1.201s
user	0m1.387s
sys	0m0.572s
[master 9f6dffd] begin prepare to build angular
 2 files changed, 50 insertions(+)

real	0m3.451s
user	0m6.024s
sys	0m0.738s
warning: in the working copy of 'public/3rdpartylicenses.txt', CRLF will be replaced by LF the next time Git touches it
[master 244a7c4] end prepare to build angular
 2 files changed, 29 insertions(+), 1 deletion(-)
warning: in the working copy of 'public/3rdpartylicenses.txt', CRLF will be replaced by LF the next time Git touches it
[master c731f0e] begin prepare to unit test angular
 2 files changed, 27 insertions(+), 33 deletions(-)

real	0m4.030s
user	0m22.468s
sys	0m3.697s
[master 90d9e15] end prepare to unit test angular
 2 files changed, 60 insertions(+)

real	0m0.140s
user	0m0.095s
sys	0m0.052s
[master 8c1ddf2] add timestamp
 2 files changed, 32 insertions(+)

==> running export.sh (refresh docs/llm/dump.txt)
warning: in the working copy of 'docs/llm/dump.txt', CRLF will be replaced by LF the next time Git touches it
[master 4eae1a5] add repo context dump
 1 file changed, 240 insertions(+), 240 deletions(-)

==> syncing with origin/master
From github.com:angulareighteen/angulareighteen.github.io
 * branch            master     -> FETCH_HEAD
Current branch master is up to date.
warning: in the working copy of 'docs/llm/dump.txt', CRLF will be replaced by LF the next time Git touches it
On branch master
Your branch is ahead of 'origin/master' by 13 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
Enumerating objects: 84, done.
Counting objects: 100% (84/84), done.
Delta compression using up to 16 threads
Compressing objects: 100% (44/44), done.
Writing objects: 100% (72/72), 11.21 KiB | 2.80 MiB/s, done.
Total 72 (delta 45), reused 35 (delta 16), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (45/45), completed with 10 local objects.
remote: 
remote: GitHub found 1 vulnerability on angulareighteen/angulareighteen.github.io's default branch (1 low). To find out more, visit:
remote:      https://github.com/angulareighteen/angulareighteen.github.io/security/dependabot/63
remote: 
To github.com:angulareighteen/angulareighteen.github.io.git
   7ec4450..4eae1a5  master -> master

==> done.

real	0m48.225s
user	0m39.327s
sys	0m11.430s
kushal@fedora:~/src/angular/angulareighteen.github.io$ cd ~/src/angular/angulareighteen.github.io/; cat update.sh; time bash update.sh;
#!/usr/bin/env bash
# =============================================================================
# update.sh — Toolchain update + rebuild + test + publish for this repo.
#
# Location: <repo root>/update.sh   (sits next to export.sh)
#
# Run with:   bash update.sh
#             bash /any/absolute/path/to/update.sh     # CWD does NOT matter
#
# What it does (modernised, while preserving every step of the original):
#   • Locates the repository from THIS script's own path, so the current
#     working directory is irrelevant (no hard-coded project directory).
#   • Bootstraps Node via fnm OR nvm, whichever is present (auto-detected;
#     force one with NODE_VM=fnm or NODE_VM=nvm in the environment).
#   • Operates on the CURRENT git branch — nothing is hard-coded to master.
#   • Writes its human-readable run log to docs/llm/update.md by default
#     (see UPDATE_LOG_REL below) instead of README.md.
#   • Logs unit-test output to locallog/fedoratest.md (as before).
#   • Appends network probes to ping.txt (as before).
#   • Upgrades Node to the latest LTS, installs the global @angular/cli + yarn,
#     records `ng version`, installs deps, runs `ng update`, refreshes the
#     browserslist + Baseline databases, builds for production, runs the unit
#     tests, bumps the patch version, runs export.sh to refresh
#     docs/llm/dump.txt, then rebases from and pushes to origin on the current
#     branch.
#
# ERROR-HANDLING NOTE:
#   This script intentionally does NOT use `set -e`. Like the original, it is a
#   best-effort pipeline: every checkpoint is committed and the run always
#   proceeds to the final push, even if an individual step (a flaky test, a
#   build warning treated as an error, an unreachable ping host, etc.) fails.
#   Fallible commands are individually guarded with `|| true`.
# =============================================================================

# ---------------------------------------------------------------------------
# Configuration — edit to taste
# ---------------------------------------------------------------------------
UPDATE_LOG_REL="docs/llm/update.md"      # main run log (replaces the old README.md target)
TEST_LOG_REL="locallog/fedoratest.md"    # unit-test output log
PING_LOG_REL="ping.txt"                  # network-probe log

NODE_MAX_OLD_SPACE_MB="8000"             # Node heap ceiling for the heavy Angular steps
PING_COUNT="20"                          # echo requests per probe
PING_TARGET="8.8.8.8"                    # probe target host

PROJECT_TITLE="angulareighteen"
SITE_URL="https://angulareighteen.github.io"
GH_SLUG="angulareighteen/angulareighteen.github.io"        # owner/repo used for the CI badge
WORKFLOW_FILE="firebase-hosting-merge.yml"

# Packages handed to `ng update`.
NG_UPDATE_PKGS=(@angular/core @angular/cli @angular/material)

# Node version manager selection: auto | fnm | nvm
NODE_VM="${NODE_VM:-auto}"

# ---------------------------------------------------------------------------
# 0. Resolve this script's own location, then the repo root (CWD-independent)
# ---------------------------------------------------------------------------
resolve_self() {
    local src="${BASH_SOURCE[0]}"
    if command -v readlink >/dev/null 2>&1 && readlink -f "$src" >/dev/null 2>&1; then
        readlink -f "$src"
        return
    fi
    # Portable fallback for systems without GNU `readlink -f`.
    local dir
    while [[ -h "$src" ]]; do
        dir="$(cd -P "$(dirname "$src")" >/dev/null 2>&1 && pwd)"
        src="$(readlink "$src")"
        [[ "$src" != /* ]] && src="$dir/$src"
    done
    printf '%s\n' "$(cd -P "$(dirname "$src")" >/dev/null 2>&1 && pwd)/$(basename "$src")"
}

SCRIPT_PATH="$(resolve_self)"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"
EXPORT_SH="${SCRIPT_DIR}/export.sh"

if ! REPO_ROOT="$(git -C "$SCRIPT_DIR" rev-parse --show-toplevel 2>/dev/null)"; then
    echo "ERROR: '$SCRIPT_DIR' is not inside a git repository; aborting." >&2
    exit 1
fi
cd "$REPO_ROOT" || { echo "ERROR: cannot cd into '$REPO_ROOT'." >&2; exit 1; }

CURRENT_BRANCH="$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo HEAD)"

# Absolute paths derived from the configurable relative paths above.
LOG="${REPO_ROOT}/${UPDATE_LOG_REL}"
TEST_LOG="${REPO_ROOT}/${TEST_LOG_REL}"
PING_LOG="${REPO_ROOT}/${PING_LOG_REL}"
mkdir -p "$(dirname "$LOG")" "$(dirname "$TEST_LOG")" "$(dirname "$PING_LOG")"

# Applied to every Node-heavy command below (exported once).
export NODE_OPTIONS="--max-old-space-size=${NODE_MAX_OLD_SPACE_MB}"

# ---------------------------------------------------------------------------
# Helpers
# ---------------------------------------------------------------------------
note() { printf '\n==> %s\n' "$*"; }                  # progress line to the terminal

gc() {                                                 # git checkpoint; never aborts the run
    git add -A
    git commit -m "$1" || true
}

md()         { printf '%s\n'   "$*"          >> "$LOG"; }   # append a line to the run log
md_code()    { printf '```%s\n' "${1:-}"     >> "$LOG"; }   # open a fenced block (``` or ```bash)
md_endcode() { printf '```\n'                >> "$LOG"; }   # close a fenced block

ping_first() { ping -A -D -c "$PING_COUNT" -v "$PING_TARGET" >  "$PING_LOG" 2>&1 || true; }
ping_probe() { ping -A -D -c "$PING_COUNT" -v "$PING_TARGET" >> "$PING_LOG" 2>&1 || true; }
ping_final() { ping    -D -c "$PING_COUNT" -v "$PING_TARGET" >> "$PING_LOG" 2>&1 || true; }

# --- Node version manager bootstrap (fnm OR nvm) ---------------------------
NODE_VM_ACTIVE="none"

_try_fnm() {
    command -v fnm >/dev/null 2>&1 || return 1
    local out
    # An explicit shell is faster and avoids fnm's process-tree detection.
    out="$(fnm env --shell bash 2>/dev/null)" || return 1
    eval "$out"
    NODE_VM_ACTIVE="fnm"
}

_try_nvm() {
    local nvm_sh="${NVM_DIR:-$HOME/.nvm}/nvm.sh"
    [[ -s "$nvm_sh" ]] || return 1
    # shellcheck disable=SC1090
    \. "$nvm_sh" || return 1
    command -v nvm >/dev/null 2>&1 || return 1
    NODE_VM_ACTIVE="nvm"
}

bootstrap_node_vm() {
    case "$NODE_VM" in
        fnm) _try_fnm || echo "WARN: NODE_VM=fnm requested but fnm is unavailable." >&2 ;;
        nvm) _try_nvm || echo "WARN: NODE_VM=nvm requested but nvm is unavailable." >&2 ;;
        auto|*)
            _try_fnm || _try_nvm \
                || echo "WARN: neither fnm nor nvm found; Node-dependent steps may fail." >&2 ;;
    esac
    note "Node version manager: ${NODE_VM_ACTIVE}"
}

install_use_lts() {
    case "$NODE_VM_ACTIVE" in
        fnm)
            time fnm install --lts             || true
            time fnm use lts-latest            || true
            fnm default lts-latest 2>/dev/null || true   # default for future shells
            ;;
        nvm)
            time nvm install --lts             || true
            time nvm use --lts                 || true
            ;;
        *)
            echo "WARN: no Node version manager active; skipping LTS install/use." >&2
            ;;
    esac
}

run_export() {
    if [[ -f "$EXPORT_SH" ]]; then
        note "running export.sh (refresh ${UPDATE_LOG_REL%/*}/dump.txt)"
        # export.sh tees its full dump to stdout; keep the console quiet and
        # rely on the file it writes to docs/llm/dump.txt.
        bash "$EXPORT_SH" >/dev/null 2>&1 || echo "WARN: export.sh exited non-zero." >&2
    else
        echo "WARN: export.sh not found at '$EXPORT_SH'; skipping context dump." >&2
    fi
}

# ===========================================================================
# 1. Boot: version manager + first probe + fresh run-log header
# ===========================================================================
bootstrap_node_vm

ping_first
date > "$LOG"
gc "add timestamp"

md ''
md "# ${PROJECT_TITLE}"
md ''
md ''
md "This project is live at [${SITE_URL}](${SITE_URL} \"colorado!\") thanks to Firebase."
md ''
md 'CI Status: '
md ''
md "[![Deploy to Firebase Hosting on merge](https://github.com/${GH_SLUG}/actions/workflows/${WORKFLOW_FILE}/badge.svg)](https://github.com/${GH_SLUG}/actions/workflows/${WORKFLOW_FILE})"
md ''

# ===========================================================================
# 2. System status
# ===========================================================================
md_code bash
ping_probe
gc "begin add system status"
md "System Memory"
if command -v free >/dev/null 2>&1; then
    free -h >> "$LOG" 2>&1 || true
else
    md '(free unavailable)'
fi
md "System Storage"
du -sh . >> "$LOG" 2>&1 || true
gc "add system status"
md_endcode
gc "end add system status"

# ===========================================================================
# 3. Update Node (LTS) + global tooling + install deps
# ===========================================================================
md_code bash
ping_probe
gc "begin update node"
install_use_lts
time npm install --global @angular/cli yarn || true
time yarn run ng version >> "$LOG" 2>&1 || true
time yarn >> "$LOG" 2>&1 || true
md_endcode
ping_probe
gc "end update node"

# ===========================================================================
# 4. Update Angular (output goes to the terminal, as in the original)
# ===========================================================================
gc "begin prepare to update angular"
time yarn run ng update "${NG_UPDATE_PKGS[@]}" || true
gc "end prepare to update angular"

# ===========================================================================
# 5. Refresh the browser-targeting databases
#    - caniuse-lite / browserslist   (update-browserslist-db)
#    - baseline-browser-mapping       (consulted by @angular/build; without a
#      refresh the build prints "the data in this module is over two months old")
#    `update-browserslist-db` only touches caniuse-lite, so the Baseline data is
#    refreshed separately. `yarn add -D` records it in package.json + yarn.lock;
#    npm is a fallback if yarn refuses for any reason.
# ===========================================================================
md_code bash
time npx update-browserslist-db@latest >> "$LOG" 2>&1 || true
time yarn add --dev baseline-browser-mapping@latest >> "$LOG" 2>&1 \
    || time npm install --save-dev baseline-browser-mapping@latest >> "$LOG" 2>&1 \
    || true
md_endcode

# ===========================================================================
# 6. Production build
# ===========================================================================
md_code bash
ping_probe
gc "begin prepare to build angular"
time yarn run ng build --configuration production >> "$LOG" 2>&1 || true
md_endcode
gc "end prepare to build angular"

# ===========================================================================
# 7. Unit tests (logged to the test log).
#    Angular 20+ runs unit tests with Vitest (builder: @angular/build:unit-test),
#    not Karma. `--watch=false` guarantees a single, non-interactive run in a
#    headless/CI context so the script never blocks in watch mode.
# ===========================================================================
printf '```bash\n' > "$TEST_LOG"
ping_probe
gc "begin prepare to unit test angular"
time yarn run ng test --watch=false >> "$TEST_LOG" || true
printf '```\n' >> "$TEST_LOG"
ping_probe
gc "end prepare to unit test angular"

# ===========================================================================
# 8. Version bump, context dump, and publish on the CURRENT branch
# ===========================================================================
date >> "$LOG"
ping_final
# NOTE: in Yarn Classic, `yarn version --patch` wants a clean tree and makes
# its own commit + tag. Command kept identical to the original.
time yarn version --patch >> "$LOG" 2>&1 || true
gc "add timestamp"

run_export
gc "add repo context dump"

if [[ "$CURRENT_BRANCH" == "HEAD" ]]; then
    echo "WARN: detached HEAD — skipping pull/push." >&2
else
    note "syncing with origin/${CURRENT_BRANCH}"
    git pull --rebase origin "$CURRENT_BRANCH" --strategy-option=ours || true
    gc "merge from remote"
    git push origin "$CURRENT_BRANCH" || true
fi

note "done."

==> Node version manager: fnm
warning: in the working copy of 'docs/llm/dump.txt', CRLF will be replaced by LF the next time Git touches it
[master a4a5a5b] add timestamp
 2 files changed, 25 insertions(+), 332 deletions(-)
[master 7accc09] begin add system status
 2 files changed, 38 insertions(+)
[master bb6e10e] add system status
 1 file changed, 6 insertions(+)
[master 4928b4f] end add system status
 1 file changed, 1 insertion(+)
[master 5d29e0e] begin update node
 2 files changed, 28 insertions(+)
Installing Node v24.18.0 (x64)
warning: Version already installed at "/home/kushal/.local/share/fnm/node-versions/v24.18.0"

real	0m0.100s
user	0m0.007s
sys	0m0.006s
Using Node for alias lts-latest

real	0m0.002s
user	0m0.001s
sys	0m0.000s

changed 278 packages in 2s

69 packages are looking for funding
  run `npm fund` for details
npm warn allow-scripts 1 package has install scripts not yet covered by allowScripts:
npm warn allow-scripts   yarn@1.22.22 (preinstall: :; (node ./preinstall.js > /dev/null 2>&1 || true))
npm warn allow-scripts
npm warn allow-scripts Run `npm approve-scripts --allow-scripts-pending` to review, or `npm approve-scripts <pkg>` to allow.

real	0m2.489s
user	0m2.899s
sys	0m1.710s

real	0m0.375s
user	0m0.307s
sys	0m0.123s

real	0m0.215s
user	0m0.244s
sys	0m0.048s
[master df5dd3c] end update node
 2 files changed, 70 insertions(+)
On branch master
Your branch is ahead of 'origin/master' by 6 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
yarn run v1.22.22
$ ng update @angular/core @angular/cli @angular/material
Using package manager: yarn
Collecting installed dependencies...
Found 555 dependencies.
Fetching dependency metadata from registry...
Package '@angular/core' is already up to date.
Package '@angular/cli' is already up to date.
Package '@angular/material' is already up to date.
Done in 3.10s.

real	0m3.199s
user	0m1.704s
sys	0m0.573s
On branch master
Your branch is ahead of 'origin/master' by 6 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean

real	0m1.092s
user	0m0.739s
sys	0m0.206s

real	0m1.048s
user	0m1.226s
sys	0m0.553s
[master f74ddfe] begin prepare to build angular
 2 files changed, 50 insertions(+)

real	0m3.341s
user	0m5.710s
sys	0m0.712s
warning: in the working copy of 'public/3rdpartylicenses.txt', CRLF will be replaced by LF the next time Git touches it
[master 6356404] end prepare to build angular
 2 files changed, 29 insertions(+), 1 deletion(-)
warning: in the working copy of 'public/3rdpartylicenses.txt', CRLF will be replaced by LF the next time Git touches it
[master 854db7d] begin prepare to unit test angular
 2 files changed, 27 insertions(+), 33 deletions(-)

real	0m3.882s
user	0m21.750s
sys	0m3.687s
[master 94c05db] end prepare to unit test angular
 2 files changed, 60 insertions(+)

real	0m0.152s
user	0m0.097s
sys	0m0.063s
[master 4b05234] add timestamp
 2 files changed, 32 insertions(+)

==> running export.sh (refresh docs/llm/dump.txt)
warning: in the working copy of 'docs/llm/dump.txt', CRLF will be replaced by LF the next time Git touches it
[master 69b1575] add repo context dump
 1 file changed, 240 insertions(+), 240 deletions(-)

==> syncing with origin/master
From github.com:angulareighteen/angulareighteen.github.io
 * branch            master     -> FETCH_HEAD
Current branch master is up to date.
On branch master
Your branch is ahead of 'origin/master' by 13 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
Enumerating objects: 84, done.
Counting objects: 100% (84/84), done.
Delta compression using up to 16 threads
Compressing objects: 100% (60/60), done.
Writing objects: 100% (72/72), 11.89 KiB | 2.38 MiB/s, done.
Total 72 (delta 44), reused 1 (delta 0), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (44/44), completed with 10 local objects.
remote: 
remote: GitHub found 1 vulnerability on angulareighteen/angulareighteen.github.io's default branch (1 low). To find out more, visit:
remote:      https://github.com/angulareighteen/angulareighteen.github.io/security/dependabot/63
remote: 
To github.com:angulareighteen/angulareighteen.github.io.git
   4eae1a5..69b1575  master -> master

==> done.

real	0m44.681s
user	0m37.242s
sys	0m11.245s
kushal@fedora:~/src/angular/angulareighteen.github.io$ 
